interface RenewedAccessTokenModel {
    accessToken: string;
    accessExpiration: string;
    refreshToken: string;
    refreshExpiration: string;
    userName: string;
    name: string;
    surname: string;
}